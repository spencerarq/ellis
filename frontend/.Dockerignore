# Ignora o diretório de dependências, pois elas serão instaladas dentro do container.
node_modules

# Ignora o diretório de build, pois ele será gerado dentro do container.
build

# Ignora arquivos de log dos gerenciadores de pacotes.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignora arquivos de ambiente locais.
# É uma boa prática injetar variáveis de ambiente no runtime.
.env*

# Ignora metadados do Git.
.git
.gitignore

# Ignora artefatos gerados pelos testes E2E do Playwright.
playwright-report/
test-results/
playwright/.cache/